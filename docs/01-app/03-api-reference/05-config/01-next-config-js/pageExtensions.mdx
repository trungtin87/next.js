---
title: pageExtensions
description: Mở rộng các phần mở rộng trang mặc định được sử dụng bởi Next.js khi giải quyết các trang trong Pages Router.
---

{/* The content of this doc is shared between the app and pages router. You can use the `<PagesOnly>Content</PagesOnly>` component to add content that is specific to the Pages Router. Any shared content should not be wrapped in a component. */}

<AppOnly>

Theo mặc định, Next.js chấp nhận các tệp với các phần mở rộng sau: `.tsx`, `.ts`, `.jsx`, `.js`. Điều này có thể được sửa đổi để cho phép các phần mở rộng khác như markdown (`.md`, `.mdx`).

```js filename="next.config.js"
const withMDX = require('@next/mdx')()

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['js', 'jsx', 'ts', 'tsx', 'md', 'mdx'],
}

module.exports = withMDX(nextConfig)
```

</AppOnly>

<PagesOnly>

Bạn có thể mở rộng các phần mở rộng Page mặc định (`.tsx`, `.ts`, `.jsx`, `.js`) được sử dụng bởi Next.js. Bên trong `next.config.js`, thêm cấu hình `pageExtensions`:

```js filename="next.config.js"
module.exports = {
  pageExtensions: ['mdx', 'md', 'jsx', 'js', 'tsx', 'ts'],
}
```

Thay đổi các giá trị này ảnh hưởng đến _tất cả_ các trang Next.js, bao gồm:

- [`proxy.js`](/docs/pages/api-reference/file-conventions/proxy)
- [`instrumentation.js`](/docs/pages/guides/instrumentation)
- `pages/_document.js`
- `pages/_app.js`
- `pages/api/`

Ví dụ, nếu bạn cấu hình lại các phần mở rộng trang `.ts` thành `.page.ts`, bạn sẽ cần đổi tên các trang như `proxy.page.ts`, `instrumentation.page.ts`, `_app.page.ts`.

## Bao gồm các tệp không phải trang trong thư mục `pages`

Bạn có thể đặt cùng vị trí các tệp test hoặc các tệp khác được sử dụng bởi các component trong thư mục `pages`. Bên trong `next.config.js`, thêm cấu hình `pageExtensions`:

```js filename="next.config.js"
module.exports = {
  pageExtensions: ['page.tsx', 'page.ts', 'page.jsx', 'page.js'],
}
```

Sau đó, đổi tên các trang của bạn để có phần mở rộng tệp bao gồm `.page` (ví dụ: đổi tên `MyPage.tsx` thành `MyPage.page.tsx`). Đảm bảo bạn đổi tên _tất cả_ các trang Next.js, bao gồm các tệp được đề cập ở trên.

</PagesOnly>
