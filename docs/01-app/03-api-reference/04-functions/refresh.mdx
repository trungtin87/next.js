---
title: refresh
description: Tài liệu tham khảo API cho hàm refresh.
---

`refresh` cho phép bạn làm mới client router từ bên trong một [Server Action](/docs/app/getting-started/updating-data).

## Cách sử dụng

`refresh` **chỉ** có thể được gọi từ bên trong Server Actions. Nó không thể được sử dụng trong Route Handlers, Client Components, hoặc bất kỳ ngữ cảnh nào khác.

## Tham số

```tsx
refresh(): void;
```

## Giá trị trả về

`refresh` không trả về giá trị.

## Ví dụ

```ts filename="app/actions.ts" switcher
'use server'

import { refresh } from 'next/cache'
import { redirect } from 'next/navigation'

export async function createPost(formData: FormData) {
  const title = formData.get('title')
  const content = formData.get('content')

  // Tạo bài viết trong cơ sở dữ liệu của bạn
  const post = await db.post.create({
    data: { title, content },
  })

  refresh()
}
```

```js filename="app/actions.js" switcher
'use server'

import { refresh } from 'next/cache'
import { redirect } from 'next/navigation'

export async function createPost(formData) {
  const title = formData.get('title')
  const content = formData.get('content')

  // Tạo bài viết trong cơ sở dữ liệu của bạn
  const post = await db.post.create({
    data: { title, content },
  })

  refresh()
}
```

### Lỗi khi sử dụng bên ngoài Server Actions

```ts filename="app/api/posts/route.ts" switcher
import { refresh } from 'next/cache'

export async function POST() {
  // Điều này sẽ ném ra lỗi
  refresh()
}
```
