---
title: Cách cài đặt Tailwind CSS v3 trong ứng dụng Next.js của bạn
nav_title: Tailwind CSS v3
description: Style ứng dụng Next.js của bạn bằng Tailwind CSS v3 để hỗ trợ trình duyệt rộng hơn.
---

{/* The content of this doc is shared between the app and pages router. You can use the `<PagesOnly>Content</PagesOnly>` component to add content that is specific to the Pages Router. Any shared content should not be wrapped in a component. */}

Hướng dẫn này sẽ hướng dẫn bạn cách cài đặt [Tailwind CSS v3](https://v3.tailwindcss.com/) trong ứng dụng Next.js của bạn.

> **Nên biết:** Để thiết lập Tailwind 4 mới nhất, hãy xem [hướng dẫn thiết lập Tailwind CSS](/docs/app/getting-started/css#tailwind-css).

## Cài đặt Tailwind v3

Cài đặt Tailwind CSS và các peer dependencies của nó, sau đó chạy lệnh `init` để tạo cả tệp `tailwind.config.js` và `postcss.config.js`:

```bash package="pnpm"
pnpm add -D tailwindcss@^3 postcss autoprefixer
npx tailwindcss init -p
```

```bash package="npm"
npm install -D tailwindcss@^3 postcss autoprefixer
npx tailwindcss init -p
```

```bash package="yarn"
yarn add -D tailwindcss@^3 postcss autoprefixer
npx tailwindcss init -p
```

```bash package="bun"
bun add -D tailwindcss@^3 postcss autoprefixer
bunx tailwindcss init -p
```

## Cấu hình Tailwind v3

Cấu hình các template paths của bạn trong tệp `tailwind.config.js`:

<AppOnly>

```js filename="tailwind.config.js"
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

Thêm các Tailwind directives vào tệp CSS toàn cục của bạn:

```css filename="app/globals.css"
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Import tệp CSS trong root layout của bạn:

```tsx filename="app/layout.tsx" switcher
import './globals.css'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

```jsx filename="app/layout.js" switcher
import './globals.css'

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

</AppOnly>

<PagesOnly>

```js filename="tailwind.config.js"
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

Thêm các Tailwind directives vào tệp CSS toàn cục của bạn:

```css filename="styles/globals.css"
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Import tệp CSS trong tệp `pages/_app.js` của bạn:

```jsx filename="pages/_app.js"
import '@/styles/globals.css'

export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}
```

</PagesOnly>

## Sử dụng classes

Sau khi cài đặt Tailwind CSS và thêm các global styles, bạn có thể sử dụng các utility classes của Tailwind trong ứng dụng của bạn.

<AppOnly>

```tsx filename="app/page.tsx" switcher
export default function Page() {
  return <h1 className="text-3xl font-bold underline">Hello, Next.js!</h1>
}
```

```jsx filename="app/page.js" switcher
export default function Page() {
  return <h1 className="text-3xl font-bold underline">Hello, Next.js!</h1>
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.tsx" switcher
export default function Page() {
  return <h1 className="text-3xl font-bold underline">Hello, Next.js!</h1>
}
```

```jsx filename="pages/index.js" switcher
export default function Page() {
  return <h1 className="text-3xl font-bold underline">Hello, Next.js!</h1>
}
```

</PagesOnly>

## Sử dụng với Turbopack

Kể từ Next.js 13.1, Tailwind CSS và PostCSS được hỗ trợ với [Turbopack](https://turbo.build/pack/docs/features/css#tailwind-css).
