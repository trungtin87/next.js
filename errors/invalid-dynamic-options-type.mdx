---
title: Loại tùy chọn không hợp lệ trong một lệnh gọi `next/dynamic`
---

## Tại Sao Lỗi Này Xảy Ra

Bạn có một loại tùy chọn không hợp lệ trong một lệnh gọi `next/dynamic`. Các tùy chọn phải là một object literal.

## Các Cách Có Thể Để Khắc Phục

**Trước**

```jsx filename="example.js"
import dynamic from 'next/dynamic'

const options = { loading: () => <p>...</p>, ssr: false }
const DynamicComponent = dynamic(() => import('../components/hello'), options)
```

**Sau**

```jsx filename="example.js"
import dynamic from 'next/dynamic'

const DynamicComponent = dynamic(() => import('../components/hello'), {
  loading: () => <p>...</p>,
  ssr: false,
})
```

## Các Liên Kết Hữu Ích

- [Dynamic Import](/docs/pages/guides/lazy-loading)
