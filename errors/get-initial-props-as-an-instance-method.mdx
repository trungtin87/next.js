---
title: '`getInitialProps` được định nghĩa như một phương thức instance'
---

## Tại Sao Lỗi Này Xảy Ra

`getInitialProps` phải là một phương thức tĩnh (static method) để được gọi bởi next.js.

## Các Cách Có Thể Để Khắc Phục

Sử dụng từ khóa static.

```jsx filename="pages/example.js"
export default class YourEntryComponent extends React.Component {
  static getInitialProps() {
    return {}
  }

  render() {
    return 'foo'
  }
}
```

hoặc

```jsx filename="pages/example.js"
const YourEntryComponent = function () {
  return 'foo'
}

YourEntryComponent.getInitialProps = () => {
  return {}
}

export default YourEntryComponent
```

## Các Liên Kết Hữu Ích

- [Lấy dữ liệu và vòng đời component](/docs/pages/api-reference/functions/get-initial-props)
