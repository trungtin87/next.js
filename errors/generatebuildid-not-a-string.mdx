---
title: '`generateBuildId` không trả về một chuỗi'
---

## Tại Sao Lỗi Này Xảy Ra

Nguyên nhân phổ biến nhất cho vấn đề này là một `next.config.js` tùy chỉnh với phương thức `generateBuildId` được định nghĩa, nhưng nó không trả về một chuỗi.

## Các Cách Có Thể Để Khắc Phục

Luôn trả về một chuỗi từ generateBuildId.
