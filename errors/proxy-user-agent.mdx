---
title: Đã Xóa req.ua khỏi Proxy API
---

## Tại Sao Lỗi Này Xảy Ra

Proxy của bạn đang tương tác với `req.ua` và tính năng này cần được kích hoạt (opt-in).

```ts filename="proxy.ts"
import { NextRequest, NextResponse } from 'next/server'

export function proxy(request: NextRequest) {
  const url = request.nextUrl
  const viewport = request.ua.device.type === 'mobile' ? 'mobile' : 'desktop'
  url.searchParams.set('viewport', viewport)
  return NextResponse.rewrite(url)
}
```

## Các Cách Có Thể Để Khắc Phục

Để phân tích user agent, import hàm `userAgent` từ `next/server` và truyền request của bạn vào:

```ts filename="proxy.ts"
import { NextRequest, NextResponse, userAgent } from 'next/server'

export function proxy(request: NextRequest) {
  const url = request.nextUrl
  const { device } = userAgent(request)
  const viewport = device.type === 'mobile' ? 'mobile' : 'desktop'
  url.searchParams.set('viewport', viewport)
  return NextResponse.rewrite(url)
}
```
