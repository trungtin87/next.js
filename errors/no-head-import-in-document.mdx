---
title: Không Import Head trong Document
---

> Ngăn chặn việc sử dụng `next/head` trong `pages/_document.js`.

## Tại Sao Lỗi Này Xảy Ra

`next/head` đã được import trong `pages/_document.js`. Điều này có thể gây ra các vấn đề không mong muốn trong ứng dụng của bạn.

## Các Cách Có Thể Để Khắc Phục

Chỉ import và sử dụng `next/document` trong `pages/_document.js` để ghi đè component `Document` mặc định. Nếu bạn đang import `next/head` để sử dụng component `Head`, hãy import nó từ `next/document` thay thế để sửa đổi code `<head>` trên tất cả các trang:

```jsx filename="pages/_document.js"
import Document, { Html, Head, Main, NextScript } from 'next/document'

class MyDocument extends Document {
  static async getInitialProps(ctx) {
    //...
  }

  render() {
    return (
      <Html>
        <Head></Head>
      </Html>
    )
  }
}

export default MyDocument
```

## Các Liên Kết Hữu Ích

- [Custom Document](/docs/pages/building-your-application/routing/custom-document)
