---
title: Inline script id
---

> Bắt buộc thuộc tính `id` trên các component `next/script` với nội dung inline.

## Tại Sao Lỗi Này Xảy Ra

Các component `next/script` với nội dung inline yêu cầu một thuộc tính `id` được định nghĩa để theo dõi và tối ưu hóa script.

## Các Cách Có Thể Để Khắc Phục

Thêm một thuộc tính `id` vào component `next/script`.

```jsx filename="pages/_app.js"
import Script from 'next/script'

export default function App({ Component, pageProps }) {
  return (
    <>
      <Script id="my-script">{`console.log('Hello world!');`}</Script>
      <Component {...pageProps} />
    </>
  )
}
```

## Các Liên Kết Hữu Ích

- [Tài liệu cho component Next.js Script](/docs/pages/guides/scripts)
